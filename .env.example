# 서버에서는 .env를 커밋하지 말고, 호스팅(Render/Fly/VPS)의 환경변수로 설정하는 것을 권장합니다.
# 아래 값 중 반드시 필요한 것은 CHAT_APP_PASSWORD_HASH 입니다.

# tools/set_password.py로 생성되는 해시 문자열을 그대로 넣으세요.
CHAT_APP_PASSWORD_HASH=

# (선택) 1/true/yes/on 으로 설정하면 로그인(비밀번호)을 끕니다. (공개 URL이면 권장하지 않음)
CHAT_APP_DISABLE_AUTH=

# (선택) 발신자 이름 통일(가져오기/정리 기능에 사용)
CHAT_APP_CANONICAL_ME_NAME=이성준
CHAT_APP_CANONICAL_OTHER_NAME=귀여운소연이

# (선택) Flask secret key (설정 안 하면 data 폴더에 자동 생성)
CHAT_APP_SECRET_KEY=

# (선택) 내가(오른쪽 말풍선)로 표시할 이름
CHAT_APP_ME=

# (선택) 데이터 저장 위치 (서버 영구디스크 마운트 경로로 지정 권장)
# 예: Render Disk를 /data 로 마운트 했다면 CHAT_APP_DATA_DIR=/data
CHAT_APP_DATA_DIR=

# (선택) GitHub 백업 (Render에서 자동 업로드)
# PAT(Token)에는 repo 쓰기 권한이 필요합니다.
CHAT_APP_GITHUB_TOKEN=
# 예: ssungjun83/cutesoyeon (비워두면 .git/config에서 자동 추정)
CHAT_APP_GITHUB_REPO=
# 기본값: main
CHAT_APP_GITHUB_BRANCH=
# 확장자 없이 prefix만 지정 (예: backup/chat_export)
CHAT_APP_GITHUB_BACKUP_PREFIX=

# (선택) 추억 사진 Google Drive 연동
# 서비스 계정 JSON 전체를 직접 넣거나, Base64로 인코딩해서 넣으세요.
CHAT_APP_DRIVE_SERVICE_ACCOUNT_JSON=
CHAT_APP_DRIVE_SERVICE_ACCOUNT_B64=
# 로컬에서만 파일 경로로 지정할 때 사용 (서버에서는 비권장)
CHAT_APP_DRIVE_SERVICE_ACCOUNT_FILE=
# 사진을 올릴 Drive 폴더 ID 또는 폴더 URL
CHAT_APP_DRIVE_FOLDER_ID=
CHAT_APP_DRIVE_FOLDER_URL=

# 로컬 실행용
CHAT_APP_HOST=127.0.0.1
CHAT_APP_PORT=8000

